DJANGO_SECRET_KEY=change-me-to-a-random-secret
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1

# Set to True to use local sqlite instead of MySQL (handy for quick testing)
DJANGO_USE_SQLITE=False

DB_NAME=eventify_db
DB_USER=eventify_user
DB_PASSWORD=change to your password
DB_HOST=localhost
DB_PORT=3306
